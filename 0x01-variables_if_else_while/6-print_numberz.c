#include <stdio.h>

/**
 * main - write single digit number
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
	{
	putchar (0-9);
	}
	}
	putchar ('\n');

	return (0);
}
