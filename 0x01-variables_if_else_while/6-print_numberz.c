#include <stdio.h>

/**
 * main - write single digit number
 * Return: Always 0 (Success)
 */

int main(void)
{
	int link;

	for (link = 0; link <= 9; link++)
	{
	{
	putchar (link + '0');
	}
	}
	putchar ('\n');

	return (0);
}
