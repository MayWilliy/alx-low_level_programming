#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/** more headers goes there *
 * main - Entry point of the program
 * Return: Always 0 (Success)
 */


int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	if (n >
	return (0);
}
