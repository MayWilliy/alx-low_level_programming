#include  <stdio.h>

/**
 * main - print number base of 10 start form zero
 * Return: Alwaays 0 (Success)
 */

int main(void)
{
		int i;

		for (i = 0; i <= 9; i++)
		{
		printf("%d", i);
		}
		putchar ('\n');
		return (0);
}
