#include <stdio.h>
#include <stdlib.h>

/**
 * main - print alphabet excluding q and e
 * Return: Always 0 (success)
 */

int main(void)
{
		char(x);

		for (x = 'a'; x <= 'z'; x++)
		{
		putchar (x);
		}
		if (x != 'q' && x != 'e')
		putchar('\n');
		return (0);
}
