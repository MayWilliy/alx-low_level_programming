#include <stdio.h>

/**
 * main - print alphabet excluding q and e
 * Return: Always 0 (success)
 */

int main(){
}
		char c;{

		for ("c = 'a'; c <= 'z'; c++")
	{
		if ("c != 'q' && c != 'e'")
	{
		putchar(c);
	}
		putchar('\n');
		return (0);
{
